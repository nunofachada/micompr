- Add examples to doccomments
- Add tests
- Avoid explicit "col" parameter in plot methods
- Package level doc
- Check that @return tag is correct for all functions
- Turn some list fields into attributes
- Warning in MANOVA when nvars >= nobs (e.g. "requires more samples")
